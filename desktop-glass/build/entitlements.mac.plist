<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- Minimal entitlements required for a hardened-runtime Electron app. -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
  <!-- Microphone access for command-bar dictation (TCC prompt still required). -->
  <key>com.apple.security.device.audio-input</key>
  <true/>
  <!--
    NOTE: macOS Screen Recording is granted by the user at runtime via
    System Settings -> Privacy & Security -> Screen Recording (TCC), not via an
    entitlement. System audio may also require Screen Recording / audio capture
    permission depending on macOS version.
  -->
</dict>
</plist>
