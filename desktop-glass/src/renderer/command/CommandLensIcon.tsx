import { ScanEye } from "lucide-react";

/** IIVO Lens — Lucide ScanEye icon. */
export function CommandLensIcon(): JSX.Element {
  return (
    <ScanEye
      className="command-lens-btn__icon"
      size={18}
      strokeWidth={2}
      aria-hidden="true"
    />
  );
}
