import { Languages } from "lucide-react";

/** Live Translate — Lucide Languages icon. */
export function CommandTranslateIcon(): JSX.Element {
  return (
    <Languages
      className="command-translate-btn__icon"
      size={18}
      strokeWidth={2}
      aria-hidden="true"
    />
  );
}
